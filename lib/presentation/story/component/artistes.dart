import 'package:flutter/cupertino.dart';

class ArtistesSection extends StatelessWidget {
  const ArtistesSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Artistes"),);
  }
}
